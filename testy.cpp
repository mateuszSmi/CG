#include <iostream>
#include <cstdlib>
#include <time.h>
#include <cmath>

srand(time(NULL)); 

int main(int argc, char **argv)
{
	float random = ((float) rand()) / (float) RAND_MAX;
	
	return 0;
}

